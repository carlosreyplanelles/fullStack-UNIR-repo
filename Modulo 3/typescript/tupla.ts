//Tupla es una array de longitud fija y elementos de diferente tipo.
let tupla: [string, number | boolean, boolean] = ['Juan', 40, true]

let valor: string | number = 'hola'
valor = 2;

console.log(tupla);

console.log(tupla[0])